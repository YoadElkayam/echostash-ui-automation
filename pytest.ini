[pytest]
testpaths = tests
markers =
    sanity: Quick smoke tests for CI
    regression: Full regression suite
    mobile: Mobile viewport tests
    admin: Admin panel tests
    eval: Evaluation feature tests
addopts = --strict-markers
